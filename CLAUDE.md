# BetterBeads Project Rules

## Versioning

- Always bump versions in both `pyproject.toml` and `.claude-plugin/plugin.json` when making changes to this plugin
- Always push after committing a version bump so that plugin consumers get the changes
